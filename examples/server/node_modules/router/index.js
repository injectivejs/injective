module.exports = exports = Router;

function Router() {
    this.stack = {};
}

Router.prototype.handle = function(request, response) {
    this.stack[request.path](request, response);
};

Router.prototype.get = function(path, handler) {
    this.stack[path] = handler;
};